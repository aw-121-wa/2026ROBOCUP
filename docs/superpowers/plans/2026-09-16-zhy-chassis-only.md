# ZHY 基线迁移实施记录

最终范围：以 ZHY 完整运行时代码为基线，原样迁移视觉、通信、控制及主机入口；仅添加 RFID 和后续流程的底盘。用户单独确认圆盘超时继续为 20 秒。

- [x] 协议测试先在旧 Q/R 实现失败，再原样导入 ZHY 链路。
- [x] 原样导入 RDK 目录与 ZHY STM32 运行时，保留 PING/DISC/RDK_RESET 和遥测。
- [x] 添加圆盘 RFID 首次出现顺序列表、读取 API（最新要求：只存储，不设置数量门槛）；停止/错误/链路复位保留。
- [x] 20 秒到时本地停车并保留数据，协议和 RDK 五次抓取不改。
- [x] 独立 path_chassis 模块承接绕桩、台阶、仓库区域底盘动作，不发送视觉、机械臂、转仓指令。
- [x] 补充有序 UART 接收、STOP 前未处理 ID、缓冲溢出、原地 DISC 及复位测试。
- [x] 原版 RDK 55 项、CTest 33 项和部署入口测试通过；Release 固件编译成功。
- [x] 逐字节核对 RDK 全目录及原样保留的链路/控制/命令文件，无差异。

实际接口、边界和验证命令见 ../../ZHY_MIGRATION.md。未烧录。

最新调整：RFID 只作记录，接收初始化/错误/溢出诊断从底盘故障中分离；圆盘完成仅依赖 ZHY 完成反馈，20 秒保护不变。
